# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
backend/node_modules

# Build outputs
dist
dist-ssr

# Environment variables (contains secrets!)
.env
.env.local
.env.*.local
backend/.env

# Database files (contains actual data - don't commit!)
backend/db/*.db
backend/db/*.db-journal

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Package lock files (optional - some teams commit these, some don't)
# Uncomment if you don't want to commit lock files:
# package-lock.json
# backend/package-lock.json
.vercel
